{
  "name": "@liquity/contracts",
  "version": "1.0.0",
  "description": "",
  "main": "truffle-config.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "hardhat": "hardhat",
    "prepare": "run-s 'prepare:*'",
    "prepare:compile": "echo 'this fails, but is outside the scope of this repo. the upstream must fix this.' && exit 0 && hardhat compile",
    "prepare:set-version": "echo 'this fails, but is outside the scope of this repo. the upstream must fix this.' && exit 0 && node scripts/set-version.js",
    "test": "hardhat test",
    "coverage": "hardhat coverage",
    "coveralls": "cat coverage/lcov.info | coveralls"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "decimal.js": "^10.2.0",
    "eth-mutants": "^0.1.1",
    "ethereumjs-util": "^7.0.9",
    "ethers": "^5.4.3",
    "solc": "^0.6.11",
    "xmlhttprequest": "^1.8.0"
  },
  "devDependencies": {
    "@nomiclabs/hardhat-ethers": "2.0.2",
    "@nomiclabs/hardhat-etherscan": "2.1.8",
    "@nomiclabs/hardhat-truffle5": "2.0.7",
    "@nomiclabs/hardhat-web3": "2.0.0",
    "@openzeppelin/contracts": "4.9.6",
    "@openzeppelin/test-helpers": "0.5.16",
    "eth-gas-reporter": "0.2.22",
    "hardhat": "2.9.6",
    "hardhat-gas-reporter": "1.0.4",
    "npm-run-all2": "5.0.2",
    "solidity-coverage": "0.8.13",
    "web3": "1.3.4"
  }
}
